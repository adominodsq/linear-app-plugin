query TestConnection {
    teams {
        nodes {
            id
            key
            name
        }
    }
}

query GetTeamByKey {
    teams {
        nodes {
            id
            key
            name
        }
    }
}
